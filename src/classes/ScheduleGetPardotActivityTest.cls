@isTest(SeeAllData=true)
private class ScheduleGetPardotActivityTest {

    static testMethod void scheduleGetPardotActivityTest() {
    	//List<lead> leads = [Select Id, Name From Lead];
    	//delete leads;
    	/*Lead lead = new Lead();
    	lead.Email = 'rsehgal74@rediffmail.com';
    	lead.LastName = 'Test 1';
    	//lead.Is_Update_Activity__c = true;
    	insert lead;
    	Lead leadBefore = [Select Id, Name, Is_Update_Activity__c from Lead where Id =: lead.Id];
    	System.assert(leadBefore.Is_Update_Activity__c);*/
        //Test.startTest();
        //ScheduleGetPardotActivity sch1 = new ScheduleGetPardotActivity();
        //String sch = '0 0 23 * * ?';
        //system.schedule('test pardot activity', sch, sch1);
        //BatchLeadActivityUpdate job = new BatchLeadActivityUpdate();
		//database.executebatch(job, 1);
        //Test.stopTest();
        /*Lead leadAfter = [Select Id, Name, Is_Update_Activity__c from Lead where Id =: lead.Id];
        System.assert(!leadAfter.Is_Update_Activity__c);
        
        lead.Vehicle_Price__c = '3000';
        update lead;
        Lead leadAfterUpdate = [Select Id, Name, Is_Update_Activity__c from Lead where Id =: lead.Id];
        System.assert(leadAfterUpdate.Is_Update_Activity__c);*/
    }
}